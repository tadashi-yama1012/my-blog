const name = 'yama3ki';
const siteTitle = `${name}'s web site`;
const profileIconAlt = `${name}`;
const siteUrl = 'https://yama3ki.work';

export {
    name,
    siteTitle,
    profileIconAlt,
    siteUrl,
};